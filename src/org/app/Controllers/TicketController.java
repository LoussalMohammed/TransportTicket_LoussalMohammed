package org.app.Controllers;

import org.app.Services.Admin.TicketServices;
import org.views.admin.ticket.TicketView;
/*
public class TicketController {
    /*private TicketServices ticketService;
    private TicketView ticketView;

    public TicketController() {
        this.ticketService = new TicketService();
        this.ticketView = new TicketView();
    }

    public void createTicket() {
        // Logic to create a ticket
        ticketService.createTicket();
        ticketView.displayCreateSuccess();
    }

    public void viewTickets() {
        // Logic to view tickets
        List<Ticket> tickets = ticketService.getAllTickets();
        ticketView.displayTickets(tickets);
    }
}*/
